'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _StringUtils = require('./StringUtils');

var _StringUtils2 = _interopRequireDefault(_StringUtils);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var RoutingUtils = {

  openNewTab: function openNewTab(hash, params) {
    var currentLocation = window.location.href;
    currentLocation = currentLocation.split('#')[0];
    if (params !== undefined) {
      window.open(currentLocation + '#/' + _StringUtils2.default.replaceRouteVariables(hash, params), '_blank');
    } else {
      window.open(currentLocation + '#/' + hash, '_blank');
    }
  },

  getParameterByName: function getParameterByName(name, url) {
    if (!url) {
      url = window.location.href;
    }
    name = name.replace(/[\[\]]/g, '\\$&');
    var regex = new RegExp('[?&]' + name + '(=([^&#]*)|&|#|$)');
    var results = regex.exec(url);
    if (!results) {
      return null;
    }
    if (!results[2]) {
      return '';
    }
    return decodeURIComponent(results[2].replace(/\+/g, ' '));
  },

  toQueryString: function toQueryString(obj) {
    var parts = [];
    for (var i in obj) {
      if (obj.hasOwnProperty(i)) {
        parts.push(encodeURIComponent(i) + '=' + encodeURIComponent(obj[i]));
      }
    }
    return parts.join('&');
  }
};

exports.default = RoutingUtils;
//# sourceMappingURL=RoutingUtils.js.map